import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

static const primaryColor = Color(0XFF0C54BE);
static const primaryDarkColor = Color(0XFF303F60);
static const accentColor = Color(0XFFF5F9FD);
static const accentDarkColor = Color(0XFFCED3DC);
static const whiteColor = Color(0XFFFFFFFF);
}